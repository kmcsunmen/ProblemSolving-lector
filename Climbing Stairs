int climbStairs(int n) {
    if(n==1) return 1;
    else{
        int* steps = (int*)malloc(sizeof(int) * (n+1));
        steps[1] = 1, steps[2] = 2;
        for(int i=3; i<=n; i++){
            steps[i] = steps[i-2] + steps[i-1];
        }
        return steps[n];
    }
}
